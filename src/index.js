import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Đảm bảo file App.js tồn tại

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
